apply plugin: "org.springframework.boot"


dependencies {
    implementation project(':00-rewards-common')

    //-02 : Refactor from discrete Spring Framework
    // dependency to JDBC Boot Starter
    implementation 'org.springframework:spring-boot-starter-data-jdbc'
    
}

// Create original jar file with "gradlew assemble"
jar {
    enabled = true
    archiveClassifier = 'original'
}